# postgre datasource
spring:
  datasource:
    url: jdbc:mysql://containers-us-west-116.railway.app:6085/railway
    username: root
    password: o0SdodJ8MYvyDfg1YVFo
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  output:
    ansi:
      enabled: always

# logging sql
logging:
  level:
    org:
      hibernate:
        SQL: debug

jwt:
  app:
    jwtRefreshExpirationMs: 64800000

server:
  error:
    include-stacktrace: never
    include-message: always
